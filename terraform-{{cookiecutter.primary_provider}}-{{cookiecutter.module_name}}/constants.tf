#// this file contains constant values, which may be used if `default` variable
#// values are not sufficent or the constant is not exposed as input variable.
#// https://www.terraform.io/language/values/locals

locals {}
